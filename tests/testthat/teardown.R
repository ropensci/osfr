# Return osf_pat to the system environment variable
Sys.setenv(OSF_PAT = osf_pat)

# Return osf_use_server_status to the system environment variable
Sys.setenv(OSF_USE_SERVER = osf_use_server_status)
